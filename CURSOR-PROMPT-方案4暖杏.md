# Cursor 改版任务：自动上滑 → 方案 4「暖杏双折」风格

请把本 App（自动上滑 / AutoSwipe）的 **Launcher 图标** 和 **整站 UI 主题** 统一改成「暖杏双折」风格。这不是只换一个图标：深色科技风（`#101218` / `#FF7A45`）整体退场，改为 **浅色、圆润、消费级小工具** 气质。

---

## 0. 产品一句话（勿跑偏）

本机工具：用户点「开」后按随机间隔自动向上滑动，用来刷短视频。  
安静、好认、好用。不是社交、不是视频平台、不是电商。

---

## 1. 目标视觉（方案 4）

### 1.1 气质关键词
暖杏、奶油纸、圆润、友好、轻量工具、克制趣味。  
**不要：** 深黑仪表盘、霓虹、赛博、玻璃拟物堆砌、短视频品牌元素、冷灰科技感。

### 1.2 新色彩 Token（替换旧主题）

| Token | Hex | 用途 |
|--------|------|------|
| `--bg` | `#FFF6EE` | 页面大背景（暖奶油） |
| `--bg-elevated` | `#FFFFFF` | 卡片/底栏抬升面 |
| `--bg-soft` | `#FFE8D6` | 次级区块、选中浅底 |
| `--ink` | `#2A211C` | 主文字 |
| `--ink-muted` | `#8A7A70` | 次级文字、说明 |
| `--ink-faint` | `#C4B6AB` | 占位、禁用 |
| `--accent` | `#FF8F66` | 主强调（珊瑚橙，比旧 `#FF7A45` 更软） |
| `--accent-pressed` | `#F07850` | 按下 |
| `--accent-soft` | `#FFE0D2` | 强调浅底、Chip |
| `--success` | `#3CB98A` | 运行中/成功（比旧绿略柔） |
| `--success-soft` | `#D8F5EA` | 成功浅底 |
| `--danger` | `#E85D4C` | 停止/危险 |
| `--stroke` | `#F0E2D6` | 分割线、描边 |
| `--shadow` | `rgba(42,33,28,0.08)` | 极轻阴影 |

**旧色废弃（不要再作为主主题）：** `#101218`、`#1A1F2B`、强对比 `#FF7A45` 大面积使用。若需兼容夜间模式可另做 Dark，但 **默认改为浅色暖杏**。

### 1.3 形状与字体
- 圆角偏大：按钮/卡片 `16–24dp`，开关与主 CTA 更圆润。
- 图标图形线端 **圆角胶囊**，不要锋利三角。
- 字体：系统默认即可；标题可略圆润无衬线，字重不要过黑。
- 阴影：一层轻阴影即可，禁止大投影卡片堆。

---

## 2. App 图标（必须按方案 4）

### 2.1 图形
- **主图形唯一：** 两个圆润的向上 chevron（双折），叠在一起，表示「上滑」。
- 端点圆润，体量偏胖，友好，不要细线、不要手机剪影、不要文字。
- 背景：暖杏/奶油 `#FFF0E6` 或 `#FFE8D6`（与 UI 一致）。
- 前景：珊瑚 `#FF8F66`。
- **图标内禁止出现：**「自动上滑」「AutoSwipe」文字、假商标、抖音/快手等元素。

### 2.2 Android Adaptive Icon
- 前景层：居中双 chevron，关键内容落在中心约 **66%** 安全区。
- 背景层：纯色暖杏（或极轻径向暖色，不要复杂纹理）。
- 同时检查圆形 / 圆角方裁切：双折不能被切掉。
- 导出：按项目现有 mipmap / adaptive-icon 资源替换（`ic_launcher`、foreground、background）。

### 2.3 图标生成提示词（若用 AI 出图再矢量化）
```
Soft friendly Android app icon, rounded square. Warm cream/apricot background #FFF0E6.
Centered plump double upward chevrons in soft coral #FF8F66, rounded ends, consumer utility style.
Flat clean vector, no text, no phone, no neon, no dark tech background, no watermark.
Important graphics in center 66% safe zone.
```

圆形裁切再出一张做验收。

---

## 3. UI 改成同一套风格（范围）

请扫描现有界面（Compose / XML 均可），把主题与组件对齐暖杏体系，至少覆盖：

1. **启动页 / 主开关页**（开/关自动上滑）
2. **运行状态**（进行中、已暂停、失败提示）
3. **间隔/设置页**（随机间隔、权限说明等）
4. **权限引导 / 无障碍或悬浮相关说明**
5. **对话框、Snackbar/Toast、通知小图标若可跟随**
6. **设置项列表、开关、主按钮、次按钮**

### 3.1 主界面建议结构（可在现有布局上改肤+微调）
- 大背景 `--bg`
- 中央或偏上：**大号圆润主开关**（开=珊瑚实心或成功态；关=浅描边）
- 状态文案用 `--ink` / `--ink-muted`，短句，不要仪表盘数字墙
- 次要操作用文字按钮或浅底 Chip，不要黑底橙钮
- 底部可放简短说明：「本机工具，随机间隔自动上滑」

### 3.2 组件映射
| 组件 | 规格 |
|------|------|
| Primary Button | 底 `--accent`，字白或深暖白，圆角 ≥20dp，高约 48–56dp |
| Secondary | 底 `--bg-soft` 或白底 + `--stroke`，字 `--ink` |
| Switch 开 | track/thumb 走珊瑚或成功绿柔和版 |
| Card | 白/`--bg-elevated`，描边 `--stroke`，阴影极轻 |
| 运行中指示 | 小圆点 + `--success`，可配 `--success-soft` 底 |
| 危险停止 | `--danger` 只用于停止/撤销，不要大面积 |

### 3.3 文案语气
轻松克制：如「已开启」「滑一下歇一下」「需要无障碍权限才能模拟上滑」。  
不要运营腔、不要「暴刷」「撸量」暗示。

---

## 4. 实施顺序（请按此提交）

1. 定义/替换 `Color` / `Theme` / `MaterialTheme`（或 XML `colors.xml` + `themes.xml`）为上表 Token。  
2. 全局替换深色背景与旧强调橙；修对比度（浅底深字）。  
3. 主开关页、设置页、对话框按新组件规范改一版。  
4. 替换 Adaptive Icon 资源并在圆形/圆角方模拟器上截图验收。  
5. 自检：小桌面图标可认双折；UI 无残留大块 `#101218`。

---

## 5. 验收清单

- [ ] 默认主题为浅色暖杏，不是深色 Material 黑橙  
- [ ] 主色为柔珊瑚 `#FF8F66` 体系，不是旧 `#FF7A45` 硬橙主导  
- [ ] 图标：奶油底 + 圆润双上 chevron；自适应裁切不缺角  
- [ ] 主开关、状态、设置、权限说明页均已换肤  
- [ ] 无短视频品牌元素；无图标内文字  
- [ ] 对比度可读（正文与背景）  

---

## 6. 不要做

- 不要只改图标不改 UI  
- 不要保留深色壳 + 浅色图标的两套割裂气质  
- 不要做成社交/相机/短视频 App 视觉  
- 不要复杂插画场景；保持「一个主图形 + 干净界面」

---

## 7. 完成后请回报

1. 改动的主题/颜色文件路径  
2. 图标资源路径  
3. 主界面与图标在模拟器上的截图（方标 + 圆裁 + 主页 UI 各一张）
